[src]
include = [
    "composer",
    "const",
    "environments",
    "noxfile.py",
    "pipelines",
    "tasks",
]
exclude = ["**/__pycache__", ".pytest_cache", ".ruff_cache", ".venv"]

[rules]
unresolved-import = "ignore"
